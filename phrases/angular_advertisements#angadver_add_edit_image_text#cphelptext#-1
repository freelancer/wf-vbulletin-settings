You may also use an image for angular advertisement which if you specify an image for it, the style and HTML contents that specfied for the angular advertisement will be ignored and only use the image.