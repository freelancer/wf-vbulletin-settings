The maximum number of tags the users other than the thread starter can apply. Even if this setting allows it, the number of tags in a thread cannot exceed the "Maximum Tags per Thread" setting.<br />
<br />
0 disables this limit. To prevent a user from tagging a thread, use user group permissions.