Your options are configured to accept private messages only from your contacts. Therefore the following users will be unable to reply:<ul>{1}</ul>To send the message anyway, <label for="cb_anyway">check the checkbox below in the <a href="#options">Additional Options</a> section</label>, otherwise remove these users' names from the recipients list.