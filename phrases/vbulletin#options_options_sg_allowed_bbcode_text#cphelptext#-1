Use this option to specify which (if any) BB code tags your visitors may use when composing social group messages.

By default, Basic (B, I, U), Color and Link (URL, EMAIL, THREAD, POST) tags are enabled.