Below is a list of all sets of custom forum permissions.  To delete a set of custom permissions simply check the corresponding check box on the left and then click the "Delete Selected Permissions" button at the bottom.  To edit the permissions for an existing set simply click the corresponding [Edit] link on the right.