The sorting order for sets. Sets with lower values will be shown before sets with higher values.<br />
<br />
Note that the display order setting for individual prefixes only controls their order within the set.