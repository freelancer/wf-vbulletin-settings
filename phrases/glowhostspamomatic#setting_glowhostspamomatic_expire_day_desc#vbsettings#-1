Remote query expiry age in days<br>www.StopForumSpam.com lists IP/usernames/email addresses that date back some time (in some cases, several months). This option will expire old records when doing queries.<br><br>Small expiry could result in persistent spammers being able to register and a high expiry age could result in innocent people being denied registration is they happen to get an IP number that a spammer used a while ago